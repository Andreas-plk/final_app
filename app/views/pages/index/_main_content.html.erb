<div id="main-content" class="col-sm-9 mb-3 mb-sm-0 bg-white d-flex flex-wrap align-items-start">
  <h3 class="page-name"><%= link_to 'Hobby', hobby_posts_path,class:"btn btn-outline-dark btn-lg" %></h3>
  <div class="row">
    <hr class="border border-dark border-3 opacity-100">
    <%= render @hobby_posts %>
    <%= render no_posts_partial_path(@hobby_posts) %>
  </div>
  <!-- row -->
  <h3 class="page-name"><%= link_to 'Study', study_posts_path ,class:"btn btn-outline-dark btn-lg"%></h3>
  <div class="row">
    <hr class="border border-dark border-3 opacity-100">
    <%= render @study_posts %>
    <%= render no_posts_partial_path(@study_posts) %>
  </div>
  <!-- row -->
  <h3 class="page-name"><%= link_to 'Team member', team_posts_path,class:"btn btn-outline-dark btn-lg" %></h3>
  <div class="row">
    <hr class="border border-dark border-3 opacity-100">
    <%= render @team_posts %>
    <%= render no_posts_partial_path(@team_posts) %>
    <hr class="border border-dark border-3 opacity-100">
  </div>
  <!-- row -->
</div>